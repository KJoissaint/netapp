# ASP.NET MVC Application

This is a base **ASP.NET MVC** application created using the .NET CLI.  
It provides a clean starting point that can be extended with authentication, databases, APIs, or additional features.

---

## 🛠 Tech Stack

- **.NET** (ASP.NET MVC)
- **C#**
- **Razor Views**
- **Bootstrap** (default scaffold)
- **Kestrel Web Server**

---

## 📋 Prerequisites

Make sure you have the following installed:

- **.NET SDK 8.0** (or the version specified by the project)

Check your SDK version:
```bash
dotnet --version
